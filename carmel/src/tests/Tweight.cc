#include "../weight.cc"
main()
{
  Weight a,b;
  while(cin) {
    cin >> a >> b;
    cout << "a=" << a << " b=" << b << " a*b=" << a*b << " a/b=" << a/b << " a+b=" << a+b << " a-b=" << a-b << endl;
    
  }
}
