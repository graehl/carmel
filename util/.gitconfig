[user]
	name = graehl
	email = graehl@gmail.com
[github]
	user = graehl
	token = c16024811fee18c08ac842ba3d4547c5
[remote "optfunc"]
	url = git@github.com:graehl/optfunc/optfunc.py.git
	fetch = +refs/heads/*:refs/remotes/optfunc/*
[svn]
	authorsfile = /Users/graehl/t/graehl/util/.svn.authorsfile
[color]
	ui = on
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = green
    untracked = cyan

[alias]
    what = whatchanged
    st = status
    ci = commit
    br = branch
    co = checkout
    df = diff
    dc = diff --cached
    lg = log -p
    lol = log --graph --decorate --pretty=oneline --abbrev-commit
    lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
    ls = ls-files
    info = git rev-parse HEAD
    # Show files ignored by git:
    ign = ls-files -o -i --exclude-standard
[core]
	#autocrlf = true # suck it, windows people
[alias]
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
	lcrev = log --reverse --no-merges --stat @{1}..
[push]
	default = matching
[core]
	ignorecase = true
	excludesfile = /Users/graehl/.gitignore_global
[diff]
	algorithm = patience
	renameLimit = 500
[merge]
	conflictstyle = diff3
	rename-threshold = 80
